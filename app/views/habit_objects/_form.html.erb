<%= form_with(model: habit_object, local: true) do |form| %>
  <% if habit_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(habit_object.errors.count, "error") %> prohibited this habit_object from being saved:</h2>

      <ul>
        <% habit_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
